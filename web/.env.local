ADMIN_TOKEN=B@dger77!vibeprinting123

# Path overrides: when running via `npm --workspace web run dev`, cwd is web/
# so we must explicitly point these at the repo root one level up.
VP_BRANDS_DIR=../brands
VP_DATA_DIR=../data
VP_LOGS_DIR=../logs
OUTPUT_DIR=../output
