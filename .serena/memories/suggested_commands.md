# Suggested commands
- Install deps: `npm install`
- Run the planning bootstrap: `npm run plan`
- Type-check/build: `npm run build`
- Project check alias: `npm run check`
- Show repo status: `git status --short`
- List files in PowerShell: `Get-ChildItem -Force`
- Read a file in PowerShell: `Get-Content -Raw <path>`